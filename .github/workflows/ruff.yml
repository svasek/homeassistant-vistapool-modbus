name: Ruff Lint

on:
  workflow_dispatch:
  push:
    paths:
      - "custom_components/vistapool/**"
      - "tests/**"
      - "pyproject.toml"
      - "requirements-dev.txt"
      - ".github/workflows/ruff.yml"
  pull_request:
    paths:
      - "custom_components/vistapool/**"
      - "tests/**"
      - "pyproject.toml"
      - "requirements-dev.txt"
      - ".github/workflows/ruff.yml"

permissions:
  contents: read

jobs:
  ruff-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.13

      - name: Install Ruff
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade ruff

      - name: Run Ruff format check
        run: ruff format --check custom_components/vistapool tests

      - name: Run Ruff lint check
        run: ruff check custom_components/vistapool tests
